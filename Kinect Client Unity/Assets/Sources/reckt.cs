using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class reckt : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(this.gameObject, (float)0.2);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
